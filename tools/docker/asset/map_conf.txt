char_ip: char
map_ip: 127.0.0.1

// Inter-server auth — must match conf/import/char_conf.txt + the login
// account_id=1 row (rathena/sql-files/z_seroja_server_account.sql). DEV values;
// prod rotates (DEPLOY.md). PLAN-015.
userid: seroja_srv
passwd: seroja_dev

// SeROja MOTD (PLAN-015). Content lives in conf/import/motd_seroja.txt so the stock
// conf/motd.txt is never edited.
motd_txt: conf/import/motd_seroja.txt

// SeROja channels (PLAN-018 D6b). Overrides map_athena.conf's
// `channel_conf: conf/channels.conf`. The file is the whole channel_config
// block (rAthena does not merge it) — #main auto-join + #map auto-join.
channel_conf: conf/import/channels_seroja.conf
